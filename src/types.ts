export interface Project {
  id: string;
  title: string;
  image: string;
  stack: string[];
  link: string;
}

export interface SocialLink {
  id: string;
  name: string;
  icon: string;
  link: string;
  color: string;
}
